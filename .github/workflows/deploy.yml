name: Deploy

on:
  workflow_run:
    workflows: 
      - Build
    branches:
      - master
    types:
      - completed

  deploy_staging:
    name: Deploy staging
    runs-on: ubuntu-18.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: deploy
        run: echo Deployed
